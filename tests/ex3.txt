int list IL;
float list FL;

float list read_list(int n,  int y) {

	i =  -435.7;
	int list new;
	new = NIL;
	for (i = 1; i < n ; i = i + 1) {
		int elem;
		writeln(elem);
		read(elem);
	}
	return new;
}
