int list IL;
float list FL;

int read_list(int n,  y) {

    int i;
    int list new;
    new = NIL;
    else
    for (i = 0; i < n ; i = i + n) {
        int elem;
        writeln("Digite o elemento da lista: ");
        read(elem);
    }
    return 0;
}
